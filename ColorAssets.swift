extension UIColor {

    // Any: #1A6C8C
    static let primaryCalendar = UIColor(named: "primaryCalendar")

    // Any: #E3E7EB
    //     Close to conversationToolbarButtonBackground with 4.24
    static let secondarySetting = UIColor(named: "secondarySetting")

    // Any: #ECA02A
    static let tagLabelTextOrange = UIColor(named: "tagLabelTextOrange")

    // Any: #EC7060
    static let primaryRed = UIColor(named: "primaryRed")

    // Any: #DEF1F8
    static let secondaryCalendar = UIColor(named: "secondaryCalendar")

    // Any: #98A6B1
    //     Close to defaultTextColor with 1.41
    static let defaultTextColorDateTime = UIColor(named: "defaultTextColorDateTime")

    // Any: #708290
    //     Close to defaultTextColorDark with 0.0
    //     Close to tagLabelTextGray with 2.24
    static let defaultDarkGray = UIColor(named: "defaultDarkGray")

    // Any: #3388C2
    //     Close to gradeintDarkBlue with 2.83
    static let defaultAuthPasswordTextBackgroundDark = UIColor(named: "defaultAuthPasswordTextBackgroundDark")

    // Any: #D0D6DB
    static let defaultTextColorDisabled = UIColor(named: "defaultTextColorDisabled")

    // Any: #97A6B0
    //     Close to defaultTextColorDateTime with 1.41
    static let defaultTextColor = UIColor(named: "defaultTextColor")

    // Any: #FFFCF5
    static let defaultConversationCellBackgroundRead = UIColor(named: "defaultConversationCellBackgroundRead")

    // Any: #000
    static let defaultShadow = UIColor(named: "defaultShadow")

    // Any: #99C3E0
    static let defaultAuthPasswordPlaceholderDark = UIColor(named: "defaultAuthPasswordPlaceholderDark")

    // Any: #FCFCFC
    //     Close to defaultSelectionCellBackground with 3.61
    static let conversationTextFieldBackground = UIColor(named: "conversationTextFieldBackground")

    // Any: #E3EDF4
    //     Close to dividerColor with 3.74
    static let defaultTabBackground = UIColor(named: "defaultTabBackground")

    // Any: #F7F9FB
    //     Close to defaultSwitchViewBackgroundHighlight with 4.12
    //     Close to defaultSelectionCellBackground with 4.69
    //     Close to defaultBackgroundDetailCell with 4.58
    //     Close to defaultCellBackgroundSelected with 2.45
    static let defaultBackgroundWhiteDisabled = UIColor(named: "defaultBackgroundWhiteDisabled")

    // Any: #EEF4F7
    //     Close to defaultImageBackgroundDisabled with 1.41
    //     Close to tagLabelBackgroundGray with 2.45
    static let defaultBarBackground = UIColor(named: "defaultBarBackground")

    // Any: #A7A7A7
    static let conversationAttachmentButtonSizeTextColor = UIColor(named: "conversationAttachmentButtonSizeTextColor")

    // Any: #F3F8FB
    //     Close to defaultBackgroundWhiteDisabled with 4.12
    //     Close to defaultBackgroundDetailCell with 4.9
    static let defaultSwitchViewBackgroundHighlight = UIColor(named: "defaultSwitchViewBackgroundHighlight")

    // Any: #E3EAEE
    //     Close to secondarySetting with 4.24
    //     Close to dividerColor with 3.32
    static let conversationToolbarButtonBackground = UIColor(named: "conversationToolbarButtonBackground")

    // Any: #FFF9F9
    static let defaultBuilderFieldViewErrorBackground = UIColor(named: "defaultBuilderFieldViewErrorBackground")

    // Any: #314250
    //     Close to defaultTextBlack2 with 3.74
    //     Close to primarySetting with 0.0
    static let defaultTextBlack = UIColor(named: "defaultTextBlack")

    // Any: #B9CAD5
    static let defaultBorderColor = UIColor(named: "defaultBorderColor")

    // Any: #F9FCFE
    //     Close to conversationTextFieldBackground with 3.61
    //     Close to defaultBackgroundWhiteDisabled with 4.69
    //     Close to defaultBackgroundDetailCell with 4.58
    static let defaultSelectionCellBackground = UIColor(named: "defaultSelectionCellBackground")

    // Any: #7C8D9C
    static let defaultTextSubtitle = UIColor(named: "defaultTextSubtitle")

    // Any: #FFF8E8
    static let conversationNoteBackground = UIColor(named: "conversationNoteBackground")

    // Any: #F5FAFF
    //     Close to defaultBackgroundWhiteDisabled with 4.58
    //     Close to defaultSwitchViewBackgroundHighlight with 4.9
    //     Close to defaultSelectionCellBackground with 4.58
    // Dark: #F5FAFF
    static let defaultBackgroundDetailCell = UIColor(named: "defaultBackgroundDetailCell")

    // Any: #304453
    //     Close to defaultTextBlack with 3.74
    //     Close to primarySetting with 3.74
    static let defaultTextBlack2 = UIColor(named: "defaultTextBlack2")

    // Any: #F8F8F9
    //     Close to defaultBackgroundWhiteDisabled with 2.45
    static let defaultCellBackgroundSelected = UIColor(named: "defaultCellBackgroundSelected")

    // Any: #C8D3DB
    static let defaultDashboardRoundButtonBorder = UIColor(named: "defaultDashboardRoundButtonBorder")

    // Any: #DBE5EB
    static let conversationAttachmentButtonBorder = UIColor(named: "conversationAttachmentButtonBorder")

    // Any: #F2F2F2
    static let defaultCellBackgroundHighlighted = UIColor(named: "defaultCellBackgroundHighlighted")

    // Any: #EFF3F7
    //     Close to defaultBarBackground with 1.41
    //     Close to tagLabelBackgroundGray with 2.45
    static let defaultImageBackgroundDisabled = UIColor(named: "defaultImageBackgroundDisabled")

    // Any: #D9E0E7
    static let defaultImageTintDisabled = UIColor(named: "defaultImageTintDisabled")

    // Any: #CAD2D6
    static let defaultTextColorLight = UIColor(named: "defaultTextColorLight")

    // Any: #50AAE4
    static let defaultTextHighlighted = UIColor(named: "defaultTextHighlighted")

    // Any: #708290
    //     Close to defaultDarkGray with 0.0
    //     Close to tagLabelTextGray with 2.24
    static let defaultTextColorDark = UIColor(named: "defaultTextColorDark")

    // Any: #FFFFFF
    //     Close to defaultBackgroundWhite with 0.0
    static let defaultTextWhite = UIColor(named: "defaultTextWhite")

    // Any: #E2EBF1
    //     Close to defaultTabBackground with 3.74
    //     Close to conversationToolbarButtonBackground with 3.32
    static let dividerColor = UIColor(named: "dividerColor")

    // Any: #FFFFFF
    //     Close to defaultTextWhite with 0.0
    static let defaultBackgroundWhite = UIColor(named: "defaultBackgroundWhite")

    // Any: #479FDC
    static let primaryBlue = UIColor(named: "primaryBlue")

    // Any: #F3E6BE
    static let primaryProgressSub = UIColor(named: "primaryProgressSub")

    // Any: #3186C2
    //     Close to defaultAuthPasswordTextBackgroundDark with 2.83
    static let gradeintDarkBlue = UIColor(named: "gradeintDarkBlue")

    // Any: #FCE2DE
    static let tagLabelBackgroundPink = UIColor(named: "tagLabelBackgroundPink")

    // Any: #86BE56
    //     Close to primaryGreen with 2.24
    static let tagLabelTextGreen = UIColor(named: "tagLabelTextGreen")

    // Any: #87C056
    //     Close to tagLabelTextGreen with 2.24
    static let primaryGreen = UIColor(named: "primaryGreen")

    // Any: #F9EEFF
    static let secondaryCustomer = UIColor(named: "secondaryCustomer")

    // Any: #FFEBE9
    static let secondaryInbox = UIColor(named: "secondaryInbox")

    // Any: #9777A8
    static let primaryCustomer = UIColor(named: "primaryCustomer")

    // Any: #F5D2CE
    static let tagLabelBackgroundRed = UIColor(named: "tagLabelBackgroundRed")

    // Any: #6969E0
    static let primaryTask = UIColor(named: "primaryTask")

    // Any: #314250
    //     Close to defaultTextBlack with 0.0
    //     Close to defaultTextBlack2 with 3.74
    static let primarySetting = UIColor(named: "primarySetting")

    // Any: #F0F5F8
    //     Close to defaultBarBackground with 2.45
    //     Close to defaultImageBackgroundDisabled with 2.45
    static let tagLabelBackgroundGray = UIColor(named: "tagLabelBackgroundGray")

    // Any: #E0F2FF
    static let secondaryRetailItem = UIColor(named: "secondaryRetailItem")

    // Any: #FFEAAE
    static let tagLabelBackgroundOrange = UIColor(named: "tagLabelBackgroundOrange")

    // Any: #EDAE4A
    static let primaryProgress = UIColor(named: "primaryProgress")

    // Any: #FE685A
    static let errorRed = UIColor(named: "errorRed")

    // Any: #E0F1DA
    static let tagLabelBackgroundGreen = UIColor(named: "tagLabelBackgroundGreen")

    // Any: #EDECFF
    static let secondaryTask = UIColor(named: "secondaryTask")

    // Any: #6CBAEF
    static let gradientLightBlue = UIColor(named: "gradientLightBlue")

    // Any: #6F828E
    //     Close to defaultDarkGray with 2.24
    //     Close to defaultTextColorDark with 2.24
    static let tagLabelTextGray = UIColor(named: "tagLabelTextGray")
}
